import 'package:flutter/material.dart';

/// 事业首页字体
class CarrerIndexStyles {
  // 通用12号,加粗字体
  static TextStyle commonTextStyle =
      TextStyle(fontSize: 56, fontWeight: FontWeight.bold);

  // 左边距
  static double mainContainerPadding = 12;
}

/// 进度条样式
class ProgressStyles {
  // 主条形bar的颜色
  static Color mainBarColor = Color(0xff2211);
  // 进度节点圆的 半径
  static double circleRadius;
}
